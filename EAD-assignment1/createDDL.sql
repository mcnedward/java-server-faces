CREATE TABLE artist (ARITSTID INTEGER NOT NULL, ARTIST VARCHAR(255), PRIMARY KEY (ARITSTID))
CREATE TABLE album (ALBUMID INTEGER NOT NULL, ALBUM VARCHAR(255), RELEASEYEAR VARCHAR(255), artistId INTEGER, genreId INTEGER, PRIMARY KEY (ALBUMID))
CREATE TABLE genre (GENREID INTEGER NOT NULL, GENRE VARCHAR(255), PRIMARY KEY (GENREID))
CREATE TABLE song (SONGID INTEGER NOT NULL, ALBUM VARCHAR(255), ARTIST VARCHAR(255), DURATION VARCHAR(255), TITLE VARCHAR(255), TRACK VARCHAR(255), PRIMARY KEY (SONGID))
CREATE TABLE playlist (PLAYLISTID INTEGER NOT NULL, SONGLIST BLOB(2147483647), PRIMARY KEY (PLAYLISTID))
ALTER TABLE album ADD CONSTRAINT FK_album_artistId FOREIGN KEY (artistId) REFERENCES artist (ARITSTID)
ALTER TABLE album ADD CONSTRAINT FK_album_genreId FOREIGN KEY (genreId) REFERENCES genre (GENREID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
